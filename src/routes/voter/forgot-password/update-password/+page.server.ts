import type { Actions, PageServerLoad } from "./$types";

export const load: PageServerLoad = async ({ locals: { supabase } }) => {

};

export const actions: Actions = {
    updatePassword: async ({ locals: { supabase } }) => {

    }
};